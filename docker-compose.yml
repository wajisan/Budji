services:
  budji:
    build: .
    ports:
      - "${PORT:-9012}:${PORT:-9012}"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      NODE_ENV: development
      PORT: ${PORT:-9012}
